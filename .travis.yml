os:
  - linux

dist: bionic

language: c

compiler:
  - gcc

# addons:
#   apt:
#     sources:
#       - ubuntu-toolchain-r-test
#     packages:
#       - gcc-9

script:
#   - export CC=gcc-7
  - gcc -v && cmake --version
  - mkdir build && cd build
  - cmake ..
  - cmake --build .
